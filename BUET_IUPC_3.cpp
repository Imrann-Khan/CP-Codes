#include <bits/stdc++.h>
#define ll long long int
#define fast ios::sync_with_stdio(NULL); cin.tie(0); cout.tie(0);
using namespace std;

int main()
{
    fast;
    ll tc;
    cin >> tc;
    
    while (tc--)
    {
       string a;
       cin>>a;
       
    }
    
    return 0;
}
