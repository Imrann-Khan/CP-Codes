#include<bits/stdc++.h>
using namespace std;

int main()
{
    /*int t;
    cin>>t;
    getchar();
    for(int k=1;k<=t;k++)
    {
    int c=0;
    string a;
    string b;
    cin.get(101);
    getchar();
    cin.get(b,101);
    getchar();
    for(int i=0;i<a.size();i++)
    {
        if(a[i]>=65 && a[i]<=90)
        {
            a[i]=a[i]+32;
        }
    }
    for(int i=0;i<b.size();i++)
    {
        if(b[i]>=65 && b[i]<=90)
        {
            b[i]=b[i]+32;
        }
    }
    for(int i=0;i<a.size();i++)
    {
        for(int j=0;j<b.size();j++)
        {
            if(a[i]==b[j])
            {
               c++;
               break;
            }
        }
    }
    if(c==a.size())
    {
        cout<<"YES"<<endl;
    }
    else
    {
        cout<<"NO"<<endl;
    }
    }*/
    int n;
    cin>>n;
    int a[n][n];
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            cin>>a[i][j];
        }
    }
    cout<<strlen();
    return 0;
}